% Section 3 -- left empty on purpose; only Sections 1 and 2 are written so far.
% What belongs here: give every door weight 1, so that the length of a route is
% the number of doors crossed; note that on a tree this makes the unique path
% of Proposition 1 the shortest path as well, and say what that buys and what
% it forbids (no weighted doors, no diagonal moves).
\section{Encoding the distance between adjacent cells as \texorpdfstring{$1$}{1}}
\label{sec:unit-edge-weights}

\textit{To be written.}
