% Section 4 -- left empty on purpose; only Sections 1 and 2 are written so far.
% What belongs here: Dijkstra on the unit-weight maze, then the same search
% guided by a heuristic (Manhattan and Euclidean distance to the exit), i.e.
% A*; admissibility of each on a 4-neighbour grid, why Euclidean is admissible
% but weaker than Manhattan here, and what guidance changes when the graph is
% a tree.
\section{Shortest path to the exit, with distance as heuristic guidance}
\label{sec:shortest-path}

\textit{To be written.}
