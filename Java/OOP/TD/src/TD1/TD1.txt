L3 MIASHS

INF F5 — TD1

Classes String et StringBuffer
La classe String permet de représenter des chaînes de caractères immuables.
public class String extends Object{
private char[] lesCaracteres;
…
}

Question 1.1
Définir les constructeurs suivants :
public String()
public String(char[] tab,
int d, int f)
public String(String s)

Permet d’obtenir la chaîne vide.
Permet d’obtenir une chaîne de caractères à partir d’une partie du
tableau de caractères tab, commençant en d et finissant en f. Le
caractère d’indice f est exclu.
Permet d’obtenir une chaîne à partir d’une autre chaîne (copie).

Question 1.2
Définir les méthodes suivantes :
public char charAt(int i)
public int length()
public String substring(int d,
int f)
public String substring(int d)
public int compareTo(String s)

Retourne le caractère d’indice i.
Retourne la longueur de la chaîne (nombre de caractères).
Retourne la sous-chaîne depuis d jusqu’à f exclu.
Retourne la sous-chaîne depuis l’indice d jusqu’à la fin.
Compare this à s. Renvoie une valeur négative, nulle ou
positive. La comparaison est une comparaison
lexicographique.
Compare la chaîne à un objet et retourne true en cas
d’égalité, false sinon.
Retourne true si et seulement si prefixe est un préfixe
de la chaîne.
Retourne la chaîne résultant de la concaténation de this et s.
Retourne l’indice de la première occurrence de c dans
this, ou -1 si c n’apparaît pas dans this.
Retourne l’indice de la première occurrence de c dans this
à partir de l’indice d, ou -1 si une telle occurrence n’existe
pas.

public boolean equals(Object
obj)
public boolean startsWith(String
prefixe)
public String concat(String s)
public int indexOf(char c)
public int indexOf(char c, int
d)

Question 1.3
Définir les méthodes de classe suivantes :
public static String valueOf
(boolean b)
public static String
valueOf(char c)
public static String
valueOf(int i)

Retourne la représentation en chaîne du booléen b.
Retourne la représentation en chaîne du caractère c.
Retourne la représentation en chaîne de l’entier i.

—1—

L3 MIASHS

INF F5 — TD1

La classe StringBuffer permet de représenter des chaînes de caractères dont la valeur
peut être modifiée.
public class StringBuffer extends Object{
private char[] lesCaracteres;
private int n; // le nombre effectif de caractères présents
// dans le tableau lesCaracteres
…
}

Question 2.1
Définir les constructeurs :
public StringBuffer(int l)
public StringBuffer()
public StringBuffer(String str)

Permet d’obtenir une instance représentant la chaîne vide,
avec un tampon de l caractères.
Permet d’obtenir une instance représentant la chaîne vide,
avec un tampon de 16 caractères.
Permet d’obtenir une instance représentant la chaîne str,
avec un tampon de str.length()+16 caractères.

Question 2.2
Définir les méthodes suivantes :
public int length()
public int capacity()
public void ensureCapacity(int
capaciteMin)
public StringBuffer append(String s)
public StringBuffer replace(int d,
int f, String s)
public StringBuffer delete(int d,
int f)
public StringBuffer insert(int d,
String s)

Retourne le nombre de caractères de la chaîne.
Retourne la capacité du tampon.
Assure que le tableau lesCaracteres a une taille
d’au moins capaciteMin éléments.
Modifie la chaîne en lui concaténant la chaîne de
caractères s.
Remplace les caractères de d à f exclu par la chaîne s.
Supprime tous les caractères d’indices d à f exclu.
Insère la chaîne s à l’indice d.

—2—

