L3 MIASHS

INF F5 — TD3

Prolog
Un programme écrit en langage Prolog est constitué de termes. Ces termes peuvent être
de différentes sortes. Nous considérons cinq sortes différentes de termes : les entiers, les
atomes, les variables et les listes. Nous nous proposons de définir des classes permettant de
créer des instances de ces différents termes. Le diagramme des classes est donné ci-dessous.
Terme
equals()
toString()

TermeAtomique

Variable

Liste

equals()
toString()

TermeFonctionnel
equals()
toString()

Entier

Atome

ListeVide

ListeNonVide

equals()
toString()

equals()
toString()

equals()
toString()

equals()
toString()

Question 1
Définir les constructeurs et public class Entier extends TermeAtomique {
private long valeur;
méthodes de la classe Entier. Un
public Entier(long valeur){...}
entier ne peut être égal qu’à un
public String toString(){...}
public boolean equals(Object obj) {...}
entier de même valeur, ou à une
}
variable dont la valeur est un entier
de même valeur.
Question 2
Définir les constructeurs et public class Atome extends TermeAtomique {
private String valeur;
méthodes de la classe Atome. Un
public Atome (String valeur) {...}
atome ne peut être égal qu’à un
public String toString(){...}
public boolean equals(Object obj) {...}
atome de même valeur, ou à une
}
variable dont la valeur est un atome
de même valeur.
Question 3
Définir les constructeurs et méthodes de la classe Variable. Une variable peut ne pas
avoir de valeur (l’attribut valeur vaut null), ou avoir une valeur (l’attribut valeur est
différent de null). La méthode toString retourne une chaîne de caractères constituée du
—1—

L3 MIASHS

nom de la variable suivi de
"=", suivi de "?" si la
variable n’a pas de valeur,
ou suivi de la valeur de la
variable convertie en chaîne
de caractères si la variable a
une valeur.

INF F5 — TD3
public class Variable extends Terme {
private String nom;
private Terme valeur;
public Variable(String nom) {...}
public Variable(String nom, Terme valeur){...}
public Terme getValeur(){...}
public String toString(){...}
public boolean equals(Object obj){...}
}

Question 4
public class ListeVide extends Liste {
Définir les constructeurs et méthodes
public ListeVide(){...}
de la classe ListeVide. La méthode
public String toString(){...}
public boolean equals(Object obj){...}
toString retourne la chaîne de
}
caractères "[]".
Question 5
Définir
les public class ListeNonVide extends Liste {
private Terme tete;
constructeurs
et
private Terme queue;
méthodes de la classe
public ListeNonVide(Terme tete, Liste queue){...}
public ListeNonVide(Terme tete, Variable queue){...}
ListeNonVide.
public String toString(){...}
Le
constructeur
public boolean equals(Object obj){...}
ayant une variable en }
deuxième
argument,
n’accepte
comme
deuxième argument qu’une variable qui n’a pas de valeur, ou une variable dont la valeur est
une liste, qui est la queue de la liste. On écrira 2 définitions de la méthode toString :
1. La méthode toString retourne la chaîne de caractères "[" + la tête de la liste
convertie en chaîne de caractères + "|" + la queue de la liste convertie en
chaîne de caractères + "]" Exemple : [1|[2|[3|[]]]]
2. La méthode toString retourne une chaîne de caractères constituée de la liste
des éléments de la liste séparés par des ',' et entre crochets.
Exemple : [1, 2, 3]
Question 6
Définir le constructeur et public class TermeFonctionnel extends Terme {
private String foncteur;
les méthodes de la classe
private Liste parametres;
TermeFonctionnel.
public TermeFonctionnel(String f, Liste p){...}
}

public String toString(){...}
public boolean equals(Object obj){...}

Question 7
Que faire pour que les chaînes de caractères obtenues par les différentes méthodes
toString() soient précédées du nom de la classe suivi de ':' .

—2—

